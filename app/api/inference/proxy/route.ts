/**
 * inference.sh proxy — required for agent-ui component.
 * Keeps INFERENCE_API_KEY server-side only.
 * Docs: https://inference.sh/docs
 */
export { GET, POST, PUT } from '@inferencesh/sdk/proxy/nextjs'
